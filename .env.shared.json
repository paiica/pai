{
  "_comment": "Single source of truth for cross-app URLs in local dev. Run `npm run env:generate` from the repo root after changing this file to rewrite every app's .env.local. Only non-secret, cross-app wiring belongs here — API keys and other secrets stay in each app's own .env.local, which is gitignored.",
  "MARKETING_URL": "http://localhost:3000",
  "PORTAL_URL": "http://localhost:3001",
  "ADMIN_URL": "http://localhost:3002",
  "PROFESSORS_URL": "http://localhost:3003",
  "PAIIEXAMS_URL": "http://localhost:3004",
  "EXAM_ADMIN_URL": "http://localhost:3005",
  "AFFILIATE_URL": "http://localhost:3006",
  "API_URL": "http://localhost:4000/api/v1"
}
